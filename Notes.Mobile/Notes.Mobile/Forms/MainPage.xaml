<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Notes.Mobile;assembly=Notes.Mobile"
             x:Class="Notes.Mobile.Forms.MainPage"
             Title="Notes">
  <ContentPage.BindingContext>
    <local:Forms.MainPageViewModel/>
  </ContentPage.BindingContext>
  
  <ListView x:Name="NotesView" ItemsSource="{Binding Notes}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <TextCell Text="{Binding Title}"></TextCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>