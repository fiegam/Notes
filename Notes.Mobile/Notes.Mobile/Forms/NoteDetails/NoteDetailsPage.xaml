<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Notes.Mobile.Forms.NoteDetails.NoteDetailsPage"
             xmlns:local="clr-namespace:Notes.Mobile;assembly=Notes.Mobile"
             Title="Note">
  <!--<ContentPage.Resources>
    <ResourceDictionary>
      <local:Converters.InverseBooleanConverter x:Key="cnvInvert"></local:Converters.InverseBooleanConverter>
    </ResourceDictionary>
  </ContentPage.Resources>-->
  
  <StackLayout Padding="15">
    <Label Text="Title"></Label>
    <Entry Text="{Binding Note.Title}" local:UnfocusedAttached.Command="{Binding SaveTitle}">
    </Entry>
    <Label Text="Body"></Label>
    <Editor Text="{Binding Note.Body}" local:UnfocusedAttached.Command="{Binding SaveBody}"></Editor>
    <Button Text="Save" Command="{Binding SaveNote}"></Button>
    <Button Text="Delete" Command="{Binding Delete}"></Button>
  </StackLayout>
</ContentPage>