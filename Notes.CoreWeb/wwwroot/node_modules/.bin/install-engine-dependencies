#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../engine-dependencies/bin/install-engine-dependencies" "$@"
  ret=$?
else 
  node  "$basedir/../engine-dependencies/bin/install-engine-dependencies" "$@"
  ret=$?
fi
exit $ret
